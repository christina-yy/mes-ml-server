services:
  - type: web
    name: mes-ml-server
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python ml_server.py"
    envVars:
      - key: DB_URL
        sync: false
      - key: PYTHON_VERSION
        value: "3.11.0"
